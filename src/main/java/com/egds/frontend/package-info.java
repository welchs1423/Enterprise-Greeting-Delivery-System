/**
 * Frontend rendering layer (Phase 14).
 *
 * <p>Provides a backend-side Vue 3 Virtual DOM serializer that wraps
 * plain text content in a JSON VNode tree before delivery.
 *
 * <p>Key components:
 * <ul>
 *   <li>{@link com.egds.frontend.Vue3VirtualDomRenderer} — serializes
 *       content as a Vue 3 VNode JSON structure</li>
 * </ul>
 */
package com.egds.frontend;
